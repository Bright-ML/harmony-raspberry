include $(LITEOSTOPDIR)/config.mk

MODULE_NAME := $(notdir $(shell pwd))

LOCAL_SRCS :=  board.c bsd_board.c

LOCAL_FLAGS :=  -I$(LITEOSTOPDIR)/../../vendor/broadcom/BCM2836/driver/uart

include $(MODULE)
